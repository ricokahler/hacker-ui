import FormControlExample from 'examples/FormControl.example';
import FormControlPlaygroundExample from 'examples/FormControlPlayground.example';
import TextInputAndTextAreaExample from 'examples/TextInputAndTextArea.example';

# Form and Input Elements

## Form Control Basics

Form Controls give form elements context.

You can place elements like `<Label />`s, `<TextInput />`s, and `<HelperText />`s inside `<FormControl />`s to synchronize their focus, error, and disabled states for one field.

<FormControlExample />

## Form Control Playground

See how you can change the elements inside a form control.

<FormControlPlaygroundExample />

## Text Inputs and Text Areas

`<TextInput />`s and `<TextArea />`s have two variants and can be controls with a `<FormControl />` element.

<TextInputAndTextAreaExample />
