# How to Style

TODO:

- detail the CSS-in-JS solution
- talk about the `color` and the `on` prop
- talk about automatic `className` and `style` drilling
- talk about "composable styles by default" via the `styles` prop
- talk about the `component` prop
- breifly talk about the `theme` (but write more about it later)
- breifly talk about the `ColorProvider` (but write more about it later)
