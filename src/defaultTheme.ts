import { Theme } from './types';

const defaultTheme: Theme = {
  colors: {
    brand: '#000',
    accent: '#2962ff',
    bland: '#ddd',
    danger: '#eb002b',
    warning: '#f56200',
    info: '#2962ff',
    surface: '#fff',
  },
};

export default defaultTheme;
