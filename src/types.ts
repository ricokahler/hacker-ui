export interface Theme {
  colors: {
    /**
     * the main brand color of your application
     */
    brand: string;
    accent: string;
    danger: string;
    warning: string;
    info: string;
    bland: string;
    surface: string;
  };
}
